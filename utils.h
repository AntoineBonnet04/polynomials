void parse (char *line, int *p1, int *p2);
// line holds the line of data and the pointers represent the 
// coefficient and exponent respectively
int powi(int x, int exp);
// exponentiate x to the power of exp
